# Curated list of YouTube video ids to FORCE-retitle.
#
# Put one video id per line. These are streams you've confirmed were mis-titled
# (e.g. a team member overwrote a Sunday worship stream with a sermon passage).
# Each id listed here is rewritten to "<its own broadcast date> - <canonical title>"
# (canonical = the first BASE_TITLES entry), regardless of its current title.
#
# Everything NOT listed here is left to the safe default: only exact BASE_TITLES
# matches get a date prefix; nothing else is touched. So adding an id is a
# deliberate, reviewable decision — keep this file in version control.
#
# The id is the part after `v=` in a watch URL, e.g.
#   https://www.youtube.com/watch?v=dQw4w9WgXcQ  ->  dQw4w9WgXcQ
#
# Blank lines and everything after a '#' are ignored. Trailing comments are fine:
#   dQw4w9WgXcQ  # 2024-03-10, was "Romans 8 - The Goodness of God"
#
# --- add ids below ---
